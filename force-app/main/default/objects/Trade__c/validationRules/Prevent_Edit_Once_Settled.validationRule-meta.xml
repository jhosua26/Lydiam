<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Prevent_Edit_Once_Settled</fullName>
    <active>true</active>
    <description>Blocks editing a Trade record once Status is Settled, except by System Administrator</description>
    <errorConditionFormula>AND(
    ISPICKVAL(Status__c, &quot;Settled&quot;),
    $Profile.Name &lt;&gt; &quot;System Administrator&quot;,
    NOT(ISNEW())
)</errorConditionFormula>
    <errorDisplayField>Status__c</errorDisplayField>
    <errorMessage>This trade has been settled and cannot be modified. Contact a System Administrator if changes are required.</errorMessage>
</ValidationRule>
